from setuptools import setup

setup(
    name="mindstorms-api",
    version="0.0.1",
    author="panchock",
    description="Api without implementation for Robot Inventor mindstorms python library",
    packages=["mindstorms"]
)
